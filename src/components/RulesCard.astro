---
interface Props {
  title?: string;
  titleAccessories?: {
    top?: string;
    bottom?: string;
  };
}

const { 
  title = "Spielregeln",
  titleAccessories = {
    top: "Foto Bingo",
    bottom: "Grandparent Edition"
  }
} = Astro.props;
---

<div class="font-serif w-full h-full p-6 bg-white">
  <div class="h-full border border-black rounded-2xl p-6 flex flex-col justify-between bg-white relative">
    
    <!-- Header -->
    <div class="text-center mt-4">
      <div class="flex items-center justify-center gap-3 mb-3 max-w-[280px] mx-auto relative">
        <div class="h-px bg-black absolute inset-x-0 top-1/2"></div>
        {titleAccessories?.top && (
          <span class="text-[10px] tracking-[0.2em] uppercase bg-white px-2 z-10">{titleAccessories.top}</span>
        )}
      </div>
      
      <h1 class="text-4xl text-black tracking-wide uppercase">{title}</h1>
      
      <div class="flex items-center justify-center gap-3 mt-3 max-w-[280px] mx-auto relative">
        <div class="h-px bg-black absolute inset-x-0 top-1/2"></div>
        {titleAccessories?.bottom && (
          <span class="text-[10px] tracking-[0.2em] uppercase bg-white px-2 z-10">{titleAccessories.bottom}</span>
        )}
      </div>
    </div>

    <!-- Rules Content -->
    <div class="flex-1 flex flex-col justify-center px-4 py-6">
      <div class="space-y-4 text-sm leading-relaxed">
        
        <div class="flex gap-3">
          <span class="font-semibold">1.</span>
          <p>
            <strong>Frag immer zuerst!</strong><br/>
            Bevor du ein Foto machst oder in versendest, frage die Person um Erlaubnis. Respekt geht vor.
          </p>
        </div>
        
        <div class="flex gap-3">
          <span class="font-semibold">2.</span>
          <p>
            <strong>Quantität vor Qualität!</strong><br/>
            Sende gerne auch Fotos in die Gruppe die nicht perfekt sind. Vielleicht erfüllst du ja auch mehr Aufgaben als es für das einfachste Bingo braucht. Ich freue mich über jedes Foto!
          </p>
        </div>
        
        <div class="flex gap-3">
          <span class="font-semibold">3.</span>
          <p>
            <strong>Ab in die Gruppe!</strong><br/>
            Scanne den QR-Code auf deiner Bingo-Karte und sende deine Beweisfotos in die WhatsApp-Gruppe. Achte darauf, die Fotos in HD Qualität zu senden: Beim Versenden links auf den Stift und dann oben auf HD tippen.
          </p>
        </div>
        
        <div class="flex gap-3">
          <span class="font-semibold">5.</span>
          <p>
            <strong>Bingo!</strong><br/>
            Wer zuerst eine Reihe, Spalte oder Diagonale voll hat, ruft laut „Bingo!" und gewinnt eine Urkunde und einen Preis. Das zweite und dritte Bingo wird ebenfalls belohnt.
          </p>
        </div>
        
      </div>
    </div>

    <!-- Footer ornament -->
    <div class="flex items-center justify-center gap-2 mb-2">
      <span class="text-lg">✦</span>
      <div class="w-12 h-px bg-black"></div>
      <span class="text-lg">✦</span>
      <div class="w-12 h-px bg-black"></div>
      <span class="text-lg">✦</span>
    </div>

  </div>
</div>
